var Browser = require("zombie");
var browser = new Browser({ debug: true });
var assert = require('chai').assert;

describe("should run the test", function()
{
    it("runs the test", function(done)
    {
        done();
    })
})